import PrerequisiteDisplay from '@site/src/components/PrerequisiteDisplay';

---
title: "روبوٹ کا مقام"
description: "روبوٹ کی پوزیشن کا تعین."
slug: "chapter-01-robot-localization"
week: 6
module: "مODULE 2: روبوٹ کا ادراک اور ماحولیاتی سمجھ"
prerequisites: ["chapter-02-feature-extraction"]
learningObjectives:
  - روبوٹ کے مقام کے تصور اور اس کی اہمیت کو سمجھیں۔
  - بےز فلٹر کے لیے بےلیف اسٹیمیٹ کے احتمالی ڈھانچے کو سمجھیں۔
  - لکیری اور تقریباً لکیری سسٹم کے لیے کلمان فلٹر اور ایکسٹینڈڈ کلمان فلٹر کے بارے میں جانیں۔
  - غیر لکیری اور متعدد ماڈل بیلیف نمائندگی کے لیے پارٹیکل فلٹر (مونٹی کارلو مقام) کو تلاش کریں۔
  - مقام کے سسٹم کے کلیدی جزوات کی شناخت کریں: موشن ماڈل، ماپنے کا ماڈل، اور نقشہ۔
  - مختلف مقام کے مسائل کے درمیان تمیز کریں، جیسے گلوبل مقام اور اغوا شدہ روبوٹ کا مسئلہ۔
estimatedTime: 180
difficultyLevel: "متوسط"
sidebar_label: "1. روبوٹ کا مقام"
position: 1
---

<PrerequisiteDisplay prerequisites={frontMatter.prerequisites} />

# 🗺️ روبوٹ کا مقام: یہ جاننا کہ آپ کہاں ہیں

ایک خود مختار روبوٹ کے لیے حقیقی دنیا میں سچی طرح سے ذہین طور پر کام کرنے کے لیے، اسے پہلے اور سب سے زیادہ ایک بنیادی سوال کا جواب دینا چاہیے: **"میں کہاں ہوں؟"** یہ **روبوٹ کا مقام** کا بنیادی مسئلہ ہے، اس کے کام کے ماحول کے اندر ایک روبوٹ کی پوزیشن اور جہت (اس کی پوز) کو درست طور پر تعین کرنے کا عمل۔ قابل اعتماد مقام کے بغیر، سب سے ترقی یافتہ راستہ منصوبہ بندی یا چیز کو ہیرا پھیری کرنے والے الگورتھم بےکار ہو جائیں گے، کیونکہ روبوٹ کو اس کی ابتدا کا نقطہ یا اس کے اعمال کو انجام دینے کا مقام نہیں معلوم ہوگا۔ 📍

یہ باب خود مختار نیویگیشن کے اہم جزو میں گہرائی سے جاتا ہے: روبوٹ کا مقام۔ ہم اس مسئلے کی بنیادی چیلنجز کو متعارف کرائیں گے اور پھر مختلف **احتمالی طریقے** تلاش کریں گے جو روبوٹس کو سینسر کے نوائز اور موشن کی عدم یقینی کے درمیان اس کی پوز کا تخمینہ لگانے کی اجازت دیتے ہیں۔ کلیدی موضوعات میں بنیادی **بےز فلٹر** شامل ہوں گے، جو ریاست کے تخمینے کے لیے ایک عام ریاضیاتی ڈھانچہ فراہم کرتے ہیں، اور ان کے زیادہ عملی اور وسیع پیمانے پر استعمال کیے جانے والے نمونے: **کلمان فلٹر** (ایکسٹینڈڈ کلمان فلٹر سمیت) جو لکیری گاؤسین سسٹم کے لیے تیار کیے گئے ہیں، اور **پارٹیکل فلٹر (مونٹی کارلو مقام)**، جو غیر لکیری، غیر گاؤسین منظر ناموں میں بہترین کارکردگی دکھاتے ہیں۔ ہم جامع طور پر تجزیہ کریں گے کہ کیسے مختلف حسی ڈیٹا (پچھلے ابواب میں تفصیل کردہ ویژن اور قریبی سینسرز سے) اور ترقی یافتہ موشن ماڈلز کو اس کی ریاست کے بارے میں روبوٹ کے یقین کو جاری طور پر اپ ڈیٹ کرنے کے لیے مربوط کیا جاتا ہے۔ ان مقام الگورتھم کی گہری سمجھ روبوٹس کے لیے ناگزیر ہے تاکہ نامعلوم، متحرک، اور پیچیدہ ماحول میں مؤثر، محفوظ، اور درست طریقے سے کام کیا جا سکے۔ اپنے روبوٹس کو اپنا راستہ تلاش کرنے میں مدد کے لیے تیار ہو جائیں! ✨

---
## روبوٹ کا مقام کیا ہے؟ یہ کیوں اہم ہے؟ 🤔

**مقام** وہ عمل ہے جس کے ذریعے ایک موبائل روبوٹ اپنے ماحول کے نقشہ کے تناظر میں اپنی پوزیشن اور جہت (اس کی "پوز") کا تعین کرتا ہے۔ ایک روبوٹ کی پوز عام طور پر 2D میں `(x, y, θ)` یا 3D میں `(x, y, z, roll, pitch, yaw)` کے ذریعے ظاہر کی جاتی ہے۔

### روبوٹکس کے لیے درست مقام کیوں اتنا اہم ہے؟
* **نیویگیشن:** اس کی موجودہ جگہ کو نہ جاننے کے باعث، ایک روبوٹ کو ایک ہدف تک جانے کا راستہ منصوبہ بند نہیں کر سکتا یا ایک راستہ پر عمل نہیں کر سکتا۔
* **کام کی انجام دہی:** بہت سے روبوٹک کام، جیسے کہ ایک چیز کو تھامنا یا ایک درست ویلڈنگ آپریشن کرنا، روبوٹ کو اس کے کام کے ماحول کے تناظر میں اس کی بالکل درست پوزیشن کو جاننے کی ضرورت ہوتی ہے۔
* **حفاظت:** درست مقام کے ذریعے سٹیٹک رکاوٹوں (نقشہ سے) اور متحرک رکاوٹوں (اگر نقشہ حقیقی وقت میں اپ ڈیٹ کیا جاتا ہے) سے تصادم کو روکنے میں مدد ملتی ہے۔
* **نقشہ کشی (SLAM):** مقام کو نقشہ کشی کے ساتھ ذاتی طور پر جوڑا گیا ہے۔ ایک نقشہ بنانے کے لیے، آپ کو یہ جاننا ہوگا کہ آپ کہاں ہیں۔ یہ جاننے کے لیے کہ آپ کہاں ہیں، اکثر آپ کو ایک نقشہ کی ضرورت ہوتی ہے۔ یہی **ہم آہنگ مقام اور نقشہ کشی (SLAM)** کا مسئلہ کا جوہر ہے، جس پر ہم اگلے باب میں بات کریں گے۔

### مقام کی چیلنج:
مقام کا مسئلہ بنیادی طور پر چیلنجز کا سامنا کرتا ہے کیونکہ:
* **سینسر نوائز:** تمام سینسرز نوائز سے متاثرہ نا مکمل پیمائش فراہم کرتے ہیں۔
* **ایکٹو ایٹر کی عدم یقینی:** روبوٹ کی حرکات کبھی بھی بالکل درست طور پر انجام نہیں دی جاتیں؛ پہیوں کی پھسلن، موٹر کی غلطیاں، اور دیگر متغیرات اوڈومیٹری میں عدم یقینی پیدا کرتے ہیں۔
* **متحرک ماحول:** ماحول میں اشیاء حرکت کر سکتی ہیں، تبدیل ہو سکتی ہیں، یا چھپ سکتی ہیں، جو موجودہ سینسر کے پڑھنے کو ایک سٹیٹک نقشہ سے میچ کرنا مشکل بنا دیتا ہے۔
* **"اغوا شدہ روبوٹ کا مسئلہ":** ایک روبوٹ ایک نامعلوم جگہ پر بیدار ہو سکتا ہے بغیر اس کی پوز کے بارے میں پہلے سے کوئی علم کے۔
* **کمپیوٹیشنل لاگت:** حقیقی وقت کا مقام کو ایسے الگورتھم کی ضرورت ہوتی ہے جو بڑی مقدار میں سینسر ڈیٹا اور پیچیدہ احتمالی تقسیم کو تیزی سے پروسیس کر سکے۔

---
## 🧮 احتمالی روبوٹکس: مقام کی بنیاد

سینسر کے پیمائش اور روبوٹ کی حرکت میں ذاتی عدم یقینی کو دیکھتے ہوئے، مقام کو **احتمالی طریقے** کا استعمال کر کے سب سے بہتر حل مل سکتا ہے۔ ایک واحد، بالکل درست پوز کا تعین کرنے کے بجائے، احتمالی مقام کا مقصد تمام ممکن روبوٹ پوزز پر ایک **یقین** (احتمالی تقسیم) برقرار رکھنا ہے۔

### بےز فلٹر ڈھانچہ:
بےز فلٹر وقت کے ساتھ ایک متحرک سسٹم (جیسے ایک روبوٹ کی پوز) کی ریاست کا تخمینہ لگانے کے لیے ایک عام ریکریسن ڈھانچہ فراہم کرتا ہے، جو سینسر کے پیمائش اور موشن کی معلومات کو ایک ساتھ ملانے کے ذریعے۔ یہ دو بنیادی اسٹیپس پر کام کرتا ہے:

1. **پیشن گوئی (موشن اپ ڈیٹ):** جب روبوٹ حرکت کرتا ہے، اس کی پوز اس کے موشن ماڈل کے مطابق تبدیل ہوتی ہے۔ یہ اسٹیپ *نئی روبوٹ کی پوز کے بارے میں یقین* کو پچھلے یقین اور روبوٹ کے کنٹرول ان پٹ (اوڈومیٹری) کی بنیاد پر *پیش نظر* کرتا ہے۔ یہ پیشن گوئی عدم یقینی میں اضافہ کرتی ہے کیونکہ حرکت کبھی بھی بالکل درست نہیں ہوتی۔
  * `Bel(xt) = ∫ P(xt | xt-1, ut) Bel(xt-1) dxt-1`
  * `P(xt | xt-1, ut)`: موشن ماڈل (حالت `xt` تک پہنچنے کا امکان پچھلی حالت `xt-1` اور کنٹرول `ut` کو دیکھتے ہوئے)۔
  * `Bel(xt-1)`: پچھلا یقین۔

2. **اپ ڈیٹ (پیمائش اپ ڈیٹ):** جب روبوٹ ایک سینسر کا پیمائش لیتا ہے، تو اس معلومات کو نئے یقین کو * corrective* یا پیش نظر یقین کو بہتر بنانے کے لیے استعمال کیا جاتا ہے۔ یہ اسٹیپ نئی شواہد کو شامل کر کے عدم یقینی کو کم کرتا ہے۔
  * `Bel(xt) = η P(zt | xt) Bel(xt)`
  * `P(zt | xt)`: پیمائش ماڈل (پیمائش `zt` کو دیکھتے ہوئے حالت `xt` کو دیکھنے کا امکان)۔
  * `η`: نارملائزیشن فیکٹر تاکہ یقین ایک احتمالی تقسیم رہے۔

یہ دو اسٹیپس مسلسل اور دہرائی کے طور پر اس وقت انجام دیے جاتے ہیں جب روبوٹ حرکت کرتا ہے اور اس کا ماحول کا ادراک کرتا ہے۔ مختلف مقام الگورتھم دراصل اس بےز فلٹر ڈھانچہ کے مختلف نافذ کاری ہیں۔

---
## مخصوص مقام الگورتھم: عملی نافذ کاری

### 1. کلمان فلٹر (KF) اور ایکسٹینڈڈ کلمان فلٹر (EKF) 📏

**کلمان فلٹر** لکیری سسٹم کے لیے ایک بہترین اسٹیمیٹر ہے جن میں گاؤسین نوائز ہو۔ یہ روبوٹ کے یقین کو ایک واحد گاؤسین تقسیم (اوسط اور کوواریئنس) کے طور پر ظاہر کرتا ہے۔

* **اصول:**
  * **پیشن گوئی:** روبوٹ کے موشن ماڈل کا استعمال کر کے روبوٹ کی پوز کا نیا اوسط اور کوواریئنس پیش نظر کرتا ہے۔
  * **اپ ڈیٹ:** سینسر کے پیمائش کا استعمال کر کے پیش نظر کردہ اوسط کو درست کرتا ہے اور کوواریئنس (عدم یقینی) کو کم کرتا ہے۔ اس میں **کلمان گین** کا حساب لگانا شامل ہوتا ہے، جو یہ تعین کرتا ہے کہ نئی پیمائش کو کتنی حد تک نئی پیمائش کے مقابلے میں اعتماد کرنا چاہیے۔
* **فوائد:**
  * **کارآمد:** صرف ایک اوسط اور ایک کوواریئنس میٹرکس کو ذخیرہ کرنے کی ضرورت ہوتی ہے، جو اسے کمپیوٹیشنل طور پر تیز بنا دیتا ہے۔
  * **بہترین:** اگر سسٹم واقعی لکیری ہو اور نوائز بالکل گاؤسین ہو، تو کے ایف اسٹیٹسٹیکل طور پر بہترین اسٹیمیٹر ہے۔
* **نقصانات:**
  * **لکیریت کا اثبات:** انتہائی غیر لکیری سسٹم (جیسے روبوٹ کی حرکت کنیمیٹکس) میں ناکام ہو جاتا ہے یا خراب کارکردگی دکھاتا ہے۔
  * **گاؤسین اثبات:** نوائز کو گاؤسین ہونے کا فرض کرتا ہے۔
  * **ایک ہیپوتھیسز:** متعدد ماڈل یقین (مثلاً، اگر روبوٹ دو مختلف مقامات پر ہونے کا امکان ہو) کو ظاہر نہیں کر سکتا۔

#### ایکسٹینڈڈ کلمان فلٹر (EKF):
غیر لکیری روبوٹ کی حرکت اور سینسر ماڈلز کو ہینڈل کرنے کے لیے، **ایکسٹینڈڈ کلمان فلٹر (EKF)** ان ماڈلز کو موجودہ تخمینہ شدہ اوسط کے گرد لکیری بنا دیتا ہے۔ یہ اسے روبوٹکس کے وسیع مسائل پر قابل اطلاق بنا دیتا ہے، لیکن یہ اب بھی ایک تقریب ہے اور اگر غیر لکیریت بہت زیادہ ہو تو بگڑ سکتا ہے۔

```python
# پائیتھن نمونہ کوڈ مثال: 1D کلمان فلٹر برائے پوزیشن ٹریکنگ (کانسیپچوئل)
# تصور کریں کہ ایک سیدھی لکیر پر حرکت کرتے ہوئے نوائز والے پیمائش کے ساتھ ایک روبوٹ کو ٹریک کرنا۔

import numpy as np
import matplotlib.pyplot as plt

class KalmanFilter1D:
    def __init__(self, initial_x, initial_p, process_noise_q, measurement_noise_r):
        self.x = initial_x  # حالت کا تخمینہ (مثلاً پوزیشن)
        self.P = initial_p  # حالت کا تخمینہ کوواریئنس (عدم یقینی)
        self.Q = process_noise_q  # عمل کا نوائز کوواریئنس
        self.R = measurement_noise_r  # پیمائش کا نوائز کوواریئنس

        # حالت کا منتقلی ماڈل (A) اور کنٹرول ان پٹ ماڈل (B)
        # مسلسل رفتار ماڈل کا فرض: x_k = x_{k-1} + v_k * dt
        # ایک سادہ پوزیشن ٹریکر کے لیے، ہم فرض کرتے ہیں x_k = x_{k-1} + u_k
        self.A = 1 # حالت کا منتقلی (x_k = A * x_{k-1})
        self.B = 1 # کنٹرول ان پٹ (x_k = ... + B * u_k)
        self.H = 1 # پیمائش ماڈل (z_k = H * x_k)

    def predict(self, u=0):
        """
        اگلی حالت اور کوواریئنس کی پیش نظری کرتا ہے۔
        u: کنٹرول ان پٹ (مثلاً روبوٹ کا کمانڈ حرکت)
        """
        self.x = self.A * self.x + self.B * u # حالت کی پیش نظری
        self.P = self.A * self.P * self.A + self.Q # کوواریئنس کی پیش نظری
        return self.x, self.P

    def update(self, z):
        """
        نئی پیمائش کے ساتھ حالت کا تخمینہ اپ ڈیٹ کرتا ہے۔
        z: موجودہ پیمائش
        """
        y = z - self.H * self.x # نو ایجاد (پیمائش کا باقی)
        S = self.H * self.P * self.H + self.R # نو ایجاد کوواریئنس
        K = self.P * self.H * (1 / S) # کلمان گین

        self.x = self.x + K * y # حالت کو اپ ڈیٹ کریں
        self.P = (1 - K * self.H) * self.P # کوواریئنس کو اپ ڈیٹ کریں
        return self.x, self.P

if __name__ == "__main__":
    # پیرامیٹرز
    initial_position = 0.0
    initial_uncertainty = 1.0 # بڑی ابتدائی عدم یقینی
    process_noise = 0.01      # روبوٹ کی حرکت کے ذریعے شامل کردہ عدم یقینی
    measurement_noise = 0.5   # سینسر کے پڑھنے میں عدم یقینی

    kf = KalmanFilter1D(initial_position, initial_uncertainty, process_noise, measurement_noise)

    # سچی روبوٹ کی حرکت اور نوائز والی پیمائش کی تقلید
    true_positions = []
    noisy_measurements = []
    kf_estimates = []
    kf_uncertainties = []

    true_x = 0.0
    for i in range(100):
        # سچی حرکت کی تقلید (مثلاً 0.1 یونٹس حرکت)
        true_x += 0.1 + np.random.normal(0, np.sqrt(process_noise))
        true_positions.append(true_x)

        # نوائز والی پیمائش کی تقلید
        measurement_z = true_x + np.random.normal(0, np.sqrt(measurement_noise))
        noisy_measurements.append(measurement_z)

        # کلمان فلٹر اسٹیپس
        kf.predict(u=0.1) # منصوبہ بند حرکت کی بنیاد پر پیش نظری
        estimated_x, estimated_p = kf.update(measurement_z)

        kf_estimates.append(estimated_x)
        kf_uncertainties.append(estimated_p)

        if (i+1) % 20 == 0:
            print(f"اسٹیپ {i+1}: سچ={true_x:.2f}, پیمائش={measurement_z:.2f}, KF_تخمینہ={estimated_x:.2f}, KF_عدم_یقین={estimated_p:.2f}")

    # نتائج کو پلاؤ کریں
    plt.figure(figsize=(12, 6))
    plt.plot(true_positions, label='سچی پوزیشن', color='blue')
    plt.plot(noisy_measurements, 'x', label='نوائز والی پیمائش', color='red', alpha=0.6)
    plt.plot(kf_estimates, label='KF تخمینہ', color='green')
    plt.fill_between(range(len(kf_estimates)),
                     np.array(kf_estimates) - 2*np.sqrt(kf_uncertainties),
                     np.array(kf_estimates) + 2*np.sqrt(kf_uncertainties),
                     color='green', alpha=0.2, label='KF عدم یقین (2σ)')
    plt.title('1D کلمان فلٹر برائے پوزیشن ٹریکنگ')
    plt.xlabel('وقت اسٹیپ')
    plt.ylabel('پوزیشن')
    plt.legend()
    plt.grid(True)
    plt.show()
    print("\n1D کلمان فلٹر کی تقلید مکمل ہو گئی۔ ✅")
```

### 2. پارٹیکل فلٹر (مونٹی کارلو مقام - MCL) ⚛️

**پارٹیکل فلٹر**، خاص طور پر روبوٹکس میں **مونٹی کارلو مقام (MCL)**، روبوٹ کے یقین کو ایک سیٹ ویighted بے ترتیب نمونے (پارٹیکلز) کے طور پر ظاہر کرتا ہے۔ ہر پارٹیکل روبوٹ کی پوز کا ایک نظریہ ہے۔

* **اصول:**
  1. **ابتداء:** نقشہ پر پارٹیکلز کو بے ترتیب طور پر تقسیم کریں (عالمی مقام) یا ایک ابتدائی تخمینہ کے گرد (ٹریکنگ)۔
  2. **پیش نظری (موشن اپ ڈیٹ):** ہر پارٹیکل کے لیے، کنٹرول ان پٹ کی بنیاد پر روبوٹ کی حرکت (نوائز کے ساتھ) کی تقلید کریں، جس سے اس پارٹیکل کے لیے ایک نئی پیش نظری شدہ پوز حاصل ہوتی ہے۔
  3. **اپ ڈیٹ (پیمائش اپ ڈیٹ):** ہر پارٹیکل کے لیے، ایک وزن کا حساب لگائیں جو یہ ظاہر کرتا ہے کہ اگر روبوٹ واقعی اس پارٹیکل کی پوز پر ہو تو موجودہ سینسر پیمائش کتنی امکان ہوگی۔ پیمائش کے ساتھ مطابقت رکھنے والے پارٹیکلز کو زیادہ وزن ملتا ہے۔
  4. **دوبارہ نمونہ دہی:** موجودہ سیٹ سے نئے پارٹیکلز کو منتخب کریں، جہاں امکان ان کے وزن کے تناسب سے ہو۔ یہ عمل "کم کر دیتا ہے" کم وزن والے پارٹیکلز اور زیادہ وزن والے پارٹیکلز کو نقل کرتا ہے، مزید قابل قبول نظریات پر کمپیوٹیشنل کوشش کو مرکوز کرتا ہے۔
* **فوائد:**
  * **غیر لکیری/غیر گاؤسین کو ہینڈل کرنا:** کسی بھی احتمالی تقسیم کو ظاہر کر سکتا ہے، جو اسے انتہائی غیر لکیری حرکت اور سینسر ماڈلز اور غیر گاؤسین نوائز کے خلاف مضبوط بنا دیتا ہے۔
  * **متعدد ماڈل یقین:** روبوٹ کی جگہ کے بارے میں متعدد الگ الگ نظریات کو ظاہر کر سکتا ہے، جو عالمی مقام اور اغوا شدہ روبوٹ کے مسئلہ کے لیے مثالی ہے۔
* **نقصانات:**
  * **کمپیوٹیشنل طور پر شدید:** درست نمائندگی کے لیے بڑی تعداد میں پارٹیکلز کی ضرورت ہوتی ہے، خاص طور پر بڑے یا پیچیدہ ماحول میں۔
  * **پارٹیکل کمی:** اگر پارٹیکلز بہت کم ہو جائیں یا اگر روبوٹ کو "اغوا" کر لیا جائے اور تمام پارٹیکلز پیمائش کے ساتھ مطابقت نہ رکھیں تو اس کا سامنا کرنا پڑ سکتا ہے۔

```python
# پائیتھن نمونہ کوڈ مثال: مونٹی کارلو مقام (MCL) (کانسیپچوئل)
# گرڈ نقشہ میں لینڈ مارکس کے ساتھ ایک سادہ 2D مقام۔

import numpy as np
import matplotlib.pyplot as plt
import random

class MCL_Robot:
    def __init__(self, x=0.0, y=0.0, heading=0.0, sensor_range=5.0, motion_noise=0.1, sensor_noise=0.5):
        self.x = x
        self.y = y
        self.heading = heading # ریڈینز
        self.sensor_range = sensor_range
        self.motion_noise = motion_noise
        self.sensor_noise = sensor_noise

    def move(self, linear_dist, angular_dist):
        self.heading += angular_dist + np.random.normal(0, self.motion_noise)
        self.x += linear_dist * np.cos(self.heading) + np.random.normal(0, self.motion_noise)
        self.y += linear_dist * np.sin(self.heading) + np.random.normal(0, self.motion_noise)
        self.heading = self.heading % (2 * np.pi) # 0 سے 2pi کے درمیان جہت رکھیں

    def sense(self, landmarks):
        """لینڈ مارکس تک فاصلے کو محسوس کرنا تقلید کریں۔"""
        measurements = []
        for lx, ly in landmarks:
            dist = np.sqrt((self.x - lx)**2 + (self.y - ly)**2)
            if dist < self.sensor_range:
                measurements.append(dist + np.random.normal(0, self.sensor_noise))
            else:
                measurements.append(float('inf')) # رینج سے باہر
        return measurements

class Particle:
    def __init__(self, x, y, heading, weight=1.0):
        self.x = x
        self.y = y
        self.heading = heading
        self.weight = weight

def motion_model(particle, linear_dist, angular_dist, motion_noise):
    """موشن ماڈل کی بنیاد پر پارٹیکل پوز کو اپ ڈیٹ کریں۔"""
    new_heading = particle.heading + angular_dist + np.random.normal(0, motion_noise)
    new_x = particle.x + linear_dist * np.cos(new_heading) + np.random.normal(0, motion_noise)
    new_y = particle.y + linear_dist * np.sin(new_heading) + np.random.normal(0, motion_noise)
    return Particle(new_x, new_y, new_heading)

def measurement_model(particle, actual_measurements, landmarks, sensor_range, sensor_noise):
    """پارٹیکل کا وزن اس کے مطابقت کے لحاظ سے حساب لگائیں۔"""
    particle_measurements = []
    for lx, ly in landmarks:
        dist = np.sqrt((particle.x - lx)**2 + (particle.y - ly)**2)
        if dist < sensor_range:
            particle_measurements.append(dist)
        else:
            particle_measurements.append(float('inf'))

    # امکان (وزن) کا حساب لگائیں
    prob = 1.0
    for i in range(len(actual_measurements)):
        if actual_measurements[i] == float('inf') and particle_measurements[i] == float('inf'):
            continue # دونوں رینج سے باہر، اچھا میچ
        if actual_measurements[i] == float('inf') or particle_measurements[i] == float('inf'):
            prob *= 0.01 # ایک رینج سے باہر، بری میچ
        else:
            # پیمائش کی غلطی کے لیے گاؤسین امکان کا فرض کریں
            error = actual_measurements[i] - particle_measurements[i]
            prob *= (1.0 / np.sqrt(2 * np.pi * sensor_noise**2)) * np.exp(-0.5 * (error**2 / sensor_noise**2))
    return prob

def normalize_weights(particles):
    total_weight = sum([p.weight for p in particles])
    if total_weight == 0:
        # صفر سے تقسیم کو روکیں، وزن کو برابر تقسیم کریں یا دوبارہ ابتداء کریں
        for p in particles:
            p.weight = 1.0 / len(particles)
        return
    for p in particles:
        p.weight /= total_weight

def resample_particles(particles):
    """وزن کے لحاظ سے نئے پارٹیکلز کے لیے دوبارہ نمونہ دہی کا چکر۔"""
    new_particles = []
    # کارآمد نمونہ لینے کے لیے نارملائزڈ وزن کا کیومولیٹو سام لیں
    weights = np.array([p.weight for p in particles])
    if np.sum(weights) == 0: # تمام وزن صفر، بے ترتیب نمونہ لینا
        return [Particle(random.uniform(0, 10), random.uniform(0, 10), random.uniform(0, 2*np.pi)) for _ in particles]

    indices = np.random.choice(len(particles), size=len(particles), p=weights)
    for i in indices:
        new_particles.append(Particle(particles[i].x, particles[i].y, particles[i].heading, 1.0/len(particles)))
    return new_particles

if __name__ == "__main__":
    # ماحول کا ترتیب
    map_size = 10 # 10x10 گرڈ
    landmarks = [(2, 8), (7, 2), (8, 8), (3, 3)] # جانے والے لینڈ مارک کی پوزیشنز
    num_particles = 1000

    # روبوٹ کو ابتداء کریں
    robot = MCL_Robot(x=1.0, y=1.0, heading=np.pi/4, motion_noise=0.1, sensor_noise=0.5)

    # پارٹیکلز کو ابتداء کریں (عالمی مقام: بے ترتیب طور پر پھیلائیں)
    particles = [Particle(random.uniform(0, map_size), random.uniform(0, map_size), random.uniform(0, 2*np.pi), 1.0/num_particles) for _ in range(num_particles)]

    robot_poses = []
    estimated_poses = []

    print("MCL تقلید شروع کر رہا ہے۔ 🗺️")
    for t in range(50): # 50 وقت کے اسٹیپس کی تقلید
        # روبوٹ حرکت کرتا ہے
        robot.move(linear_dist=0.5, angular_dist=np.pi/10)
        robot_poses.append((robot.x, robot.y))

        # روبوٹ سینس کرتا ہے
        actual_measurements = robot.sense(landmarks)

        # پارٹیکلز کے لیے MCL اسٹیپس
        for i, p in enumerate(particles):
            # پیش نظری: روبوٹ کی منصوبہ بند حرکت کی بنیاد پر پارٹیکلز کو حرکت دیں
            particles[i] = motion_model(p, linear_dist=0.5, angular_dist=np.pi/10, motion_noise=robot.motion_noise)
            # اپ ڈیٹ: سینسر کی پیمائش کی بنیاد پر وزن کا حساب لگائیں
            particles[i].weight = measurement_model(particles[i], actual_measurements, landmarks, robot.sensor_range, robot.sensor_noise)

        normalize_weights(particles)
        particles = resample_particles(particles)

        # روبوٹ کی پوز کا تخمینہ (مثلاً پارٹیکلز کا اوسط)
        est_x = np.mean([p.x for p in particles])
        est_y = np.mean([p.y for p in particles])
        estimated_poses.append((est_x, est_y))

        if (t + 1) % 10 == 0:
            print(f"اسٹیپ {t+1}: روبوٹ_سچ=({robot.x:.2f},{robot.y:.2f}), تخمینہ=({est_x:.2f},{est_y:.2f})")

    # پلاؤ کریں
    plt.figure(figsize=(10, 10))
    plt.scatter([p.x for p in particles], [p.y for p in particles], s=5, alpha=0.3, label='پارٹیکلز')
    plt.plot([rp[0] for rp in robot_poses], [rp[1] for rp in robot_poses], 'b-', label='سچا روبوٹ کا راستہ')
    plt.plot([ep[0] for ep in estimated_poses], [ep[1] for ep in estimated_poses], 'g--', label='تخمینہ شدہ روبوٹ کا راستہ')
    plt.scatter([l[0] for l in landmarks], [l[1] for l in landmarks], marker='*', s=200, color='orange', label='لینڈ مارکس')
    plt.scatter(robot.x, robot.y, marker='o', s=100, color='red', label='آخری سچا روبوٹ پوز')
    plt.scatter(est_x, est_y, marker='^', s=100, color='green', label='آخری تخمینہ شدہ روبوٹ پوز')

    plt.title('مونٹی کارلو مقام (MCL) تقلید')
    plt.xlabel('X پوزیشن')
    plt.ylabel('Y پوزیشن')
    plt.xlim(0, map_size)
    plt.ylim(0, map_size)
    plt.legend()
    plt.grid(True)
    plt.show()
    print("\nMCL تقلید مکمل ہو گئی۔ ✨")
```

---
## مقام کے سسٹم کے جزوات 🛠️

کسی بھی مخصوص الگورتھم کے بارے میں، تمام احتمالی مقام کے سسٹم کئی کلیدی جزوات پر انحصار کرتے ہیں:

* **موشن ماڈل:** وضاحت کرتا ہے کہ جب روبوٹ کو ایک کنٹرول کمانڈ (مثلاً سامنے جائیں، گھومیں) کا انجام دیا جائے تو روبوٹ کی پوز کیسے تبدیل ہوتی ہے۔ یہ ماڈل ذاتی طور پر چکنائی کے ساتھ، موٹر کی غلطیوں، وغیرہ کو بطور عدم یقینی شامل کرتا ہے (مثلاً `P(xt | xt-1, ut)`).
* **پیمائش ماڈل:** اس کے امکان کی وضاحت کرتا ہے کہ روبوٹ کی پوز اور نقشہ کو دیکھتے ہوئے ایک خاص سینسر پیمائش حاصل کرنا۔ یہ سینسر نوائز اور پیمائش کی عدم یقینی کو بطور تکلیف دیتا ہے (مثلاً `P(zt | xt)`).
* **نقشہ:** ماحول کی ایک نمائندگی، جو سٹیٹک (پہلے سے تیار) یا متحرک طور پر تیار کیا جا سکتا ہے (جیسے SLAM میں)۔ نقشہ سینسر کے پڑھنے کی تشریح کے لیے سیاق فراہم کرتے ہیں۔ اقسام میں شامل ہیں:
  * **اوقیوپنسی گرڈ نقشہ:** ماحول کو سیلز کے گرڈ کے طور پر ظاہر کرتا ہے، جو ہر ایک یہ ظاہر کرتا ہے کہ اس کا امکان بھرا ہوا ہے یا خالی ہے۔
  * **فیچر نقشہ:** ماحول میں الگ الگ لینڈ مارکس یا فیچرز کی جگہوں کو ذخیرہ کرتا ہے۔

---
## مقام کے مسائل کی اقسام 🧐

مقام کا مسئلہ مختلف اشکال میں ظاہر ہو سکتا ہے روبوٹ کے ابتدائی علم اور ماحول کے لحاظ سے۔

* **عالمی مقام:** روبوٹ اپنی ابتدائی جگہ کے بارے میں کوئی علم کے بغیر شروع ہوتا ہے۔ یہ سینسر ڈیٹا اور ایک نقشہ کا استعمال کرتے ہوئے اس کی پوز کا تعین کرنا ہوتا ہے۔ اسے اکثر MCL جیسے طریقوں کے ذریعے حل کیا جاتا ہے۔
* **اغوا شدہ روبوٹ کا مسئلہ:** عالمی مقام کا ایک متغیر جہاں روبوٹ کام کر رہا ہوتا ہے، پھر اچانک کسی نامعلوم جگہ پر "ٹیلی پورٹ" ہو جاتا ہے بغیر اس کے علم کے۔ اسے اپنے آپ کو دوبارہ مقام دینا ہوتا ہے۔ MCL اس کے لیے مضبوط ہے۔
* **مقام (ٹریکنگ):** سب سے عام منظر جہاں روبوٹ کو اس کی موجودہ پوز کا اچھا تخمینہ ہوتا ہے اور جاری طور پر اسے تبدیل کرتا ہے جیسے یہ حرکت کرتا ہے اور حس کرتا ہے۔ KF اور EKF اکثر اس کے لیے استعمال ہوتے ہیں۔

---
## 🆚 کلمان فلٹر بمقابلہ پارٹیکل فلٹر: ایک موازنہ 📈

| خصوصیت | کلمان فلٹر (KF/EKF) | پارٹیکل فلٹر (MCL) |
| :-------------------- | :----------------------------------------------- | :------------------------------------------------ |
| **یقین کی نمائندگی** | واحد گاؤسین تقسیم (اوسط + کوواریئنس) | وزن دی گئی پارٹیکلز کا سیٹ (نمونے) |
| **سسٹم کی قسم** | لکیری (KF)، تقریباً لکیری (EKF) | غیر لکیری، غیر گاؤسین |
| **نوائز کی قسم** | گاؤسین | غیر گاؤسین کو ہینڈل کر سکتا ہے |
| **کمپیوٹیشنل لاگت** | کم (مقررہ میٹرکس آپریشنز) | زیادہ (پارٹیکلز کی تعداد پر منحصر) |
| **متعدد ماڈل یقین** | ظاہر نہیں کر سکتا | ظاہر کر سکتا ہے (مثلاً عالمی مقام، اغوا شدہ روبوٹ) |
| **مضبوطی** | مضبوط غیر لکیریت کے ساتھ بگڑ سکتا ہے | انتہائی غیر لکیری ڈائینامکس کے خلاف مضبوط، اغوا شدہ روبوٹ کے خلاف مضبوط |
| ** convergance** | فرض کے مطابق تیز | convergance میں سست ہو سکتا ہے، پارٹیکل کی کثافت پر منحصر |
| **ایپلی کیشنز** | خود کار صنعت، ایئرو اسپیس، ٹریکنگ میں حالت کا تخمینہ | عالمی مقام، SLAM، انتہائی متحرک سسٹم |

---
## خاتمہ: خود مختاری کا لنگر ⚓

روبوٹ کا مقام صرف ایک کوآرڈینیٹ جاننے سے زیادہ ہے؛ یہ روبوٹ کو دنیا میں اس کی جگہ کا احساس فراہم کرنا ہے، جو ذہین، ہدف کے مطابق رویے کو ممکن بناتا ہے۔ کلمان فلٹر اور پارٹیکل فلٹر جیسے احتمالی طریقے کو مہارت حاصل کر کے، روبوٹکس ایسے سسٹم بن سکتے ہیں جو پیچیدہ، غیر یقینی ماحول میں پراعتماد طریقے سے نیویگیٹ اور کام کر سکیں۔ یہ بنیادی صلاحیت تمام اعلیٰ سطح کی خود مختاری کا لنگر کے طور پر کام کرتی ہے، نقشہ کشی، منصوبہ بندی، اور انسان-روبوٹ بات چیت میں مستقبل کی ترقی کو واقعی ممکن بناتی ہے۔ اپنی جگہ کو دریافت کرتے رہیں! 🌟